# Copy to .env and fill in your paths.
# Loaded automatically by `grave generate` and `grave eval`.

# HuggingFace model + dataset cache
HF_HOME=/mnt/E-SSD/model_cache/hf
HF_HUB_CACHE=/mnt/E-SSD/model_cache/hf
HF_DATASETS_CACHE=/mnt/E-SSD/model_cache/hf/datasets
